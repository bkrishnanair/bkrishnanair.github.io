# Portfolio application

See [the repository README](../README.md) for setup, architecture, editing, testing, and GitHub Pages publishing.

From this directory: `npm ci`, `npm run dev`, `npm test`, `npm run preview`.
